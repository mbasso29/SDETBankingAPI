internal static class TableExtensionHelpers
{
    //
    // Summary:
    //     Returns the column name that matches the given column name - if not found then
    //     the empty string is returned.
    public static string GetColumnName(this Table table, string columnName, bool throwIfNotFound = false) {
        return columnName;
    }
}